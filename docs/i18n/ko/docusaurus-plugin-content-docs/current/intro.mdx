---
sidebar_position: 1
---

# 소개

modernGraphTool은 주파수 응답 (Frequency Response, FR) 데이터를 시각화하고 분석하기 위해 설계된 최신 웹 기술 기반의 단일 페이지 애플리케이션 (Single Page Application, SPA)입니다.

## 왜 modernGraphTool인가요?

modernGraphTool은 지난 2019년에 등장하여 오랫동안 주파수 응답 데이터 시각화의 표준 도구처럼 사용되어 온 [CrinGraph](https://github.com/mlochbaum/CrinGraph)로부터 직접적인 영감을 받았습니다. 지금까지도 CrinGraph는 FR 데이터를 시각화하고 비교하는 데 널리 사용되며 많은 사랑을 받고 있습니다.

하지만 시간이 흐르면서 데이터베이스를 운영하던 관리자들은 점차 더 복잡하고 다양한 기능들을 필요로 하게 되었습니다. 자신만의 선호도가 반영된 커스텀 HRTF 타겟이 가장 대표적인 예시라고 할 수 있겠네요.

아쉽게도 CrinGraph는 초기 설계 단계에서 높은 확장성을 염두에 두지 않았기 때문에, 관리자들이 저마다 필요한 기능을 추가하는 과정에서 수많은 파생 버전이 생겨나는 파편화 문제가 발생하게 되었습니다.

이는 프로그램 유지보수를 어렵게 만들었으며, 프로그래밍 지식이 부족한 사용자가 새로운 기능을 추가하거나 다른 버전의 기능을 가져오는 것을 거의 불가능하게 만들었습니다.

modernGraphTool은 바로 이러한 문제들을 해결하고, CrinGraph의 핵심적인 장점은 계승하면서도 최신 프로그래밍 기법과 모듈화된 설계를 도입하여 안정성과 확장성을 크게 높이는 것을 목표로 합니다.

## 핵심 원칙

### 풍부한 기능을 기본 탑재

modernGraphTool은 기본 그래프 시각화부터 고급 이퀄라이저 및 하드웨어 디바이스 연동까지 포괄하는 종합적인 내장 기능을 제공하며, 모든 설정은 하나의 `config.js` 파일을 통해 관리할 수 있습니다.

핵심 스크립트를 수정해야 했던 기존 CrinGraph 파생 버전들과 달리, modernGraphTool의 기능들은 독립적인 컴포넌트로 구성되어 있어 핵심 기능에 영향을 주지 않고 활성화, 비활성화 또는 설정을 변경할 수 있습니다.

### 사용하기 쉬운 디자인

일부 CrinGraph 파생 버전들은 다양한 기능들이 추가되는 과정에서 UI가 복잡해지는 문제가 있었습니다.

특히 수평 방향으로 길게 이어지는 스크롤 영역이 여럿 배치되는 부분들은 모바일 환경에서의 사용성을 저하하기도 합니다.

modernGraphTool은 강력한 기능을 제공하는 동시에, 데스크톱과 모바일 환경 모두에서 직관적이고 편리하게 사용할 수 있도록 사용자 인터페이스(UI)와 사용자 경험(UX) 설계에 중점을 두었으며, 기능들을 명확한 패널(디바이스, 그래프, 이퀄라이저, 기타)로 분류하여 반응형 레이아웃을 적용하였습니다.

### 기존 프로그램과 호환성

modernGraphTool은 기존 CrinGraph에서 사용하던 FR 데이터의 폴더 구조와 파일 형식을 완벽하게 지원합니다.

따라서 기존에 사용하던 CrinGraph의 데이터를 그대로 modernGraphTool에 불러와서 사용할 수 있으며, 내장 연동 기능을 통해 `squig.link`와의 호환성도 보장됩니다.

## 특징

### 주파수 응답 그래프 시각화
- 정규화 기능 — Hz 기반 또는 Midrange Average (300Hz~3kHz)
- 스무딩 기능 — 1/48 oct ~ 1/3 oct
- 다중 샘플 측정 데이터 표시 (평균 또는 개별 샘플)
- HpTF (헤드폰 전달 함수) 편차 표시
- 틸트 및 선호도 조정 기능이 포함된 커스텀 HRTF 타겟 지원

### 헤드폰 / 타겟 선택 도구
- 이름 및 브랜드를 통한 검색 기능
- 헤드폰 및 타겟 보정 기능 (기준선 설정)
- 변형 및 채널 선택 (L, R, L+R, Average)
- CrinGraph 파일 구조와 호환 (`phone_book.json` & `target_manifest`)

### 이퀄라이저 & 오디오
- 다양한 필터 타입 지원 내장 파라메트릭 EQ 패널 (PK, LSQ, HSQ, Tilt)
- AutoEQ — 타겟 커브로부터 자동 필터 생성
- EQ가 적용된 실시간 오디오 재생 (Web Audio API)
- 테스트 톤 생성기
- EQ 필터 가져오기/내보내기

### 디바이스 PEQ 브리지
- 호환 하드웨어 디바이스에 EQ 설정 직접 전송
- USB HID, USB Serial, 또는 네트워크를 통한 연결
- FiiO, Qudelix-5K, Moondrop, WiiM, JDS Labs 등 다양한 기기 지원
- 다중 슬롯 프리셋 관리

### 그래프 도구
- URL 공유 기능 (+ Base62 인코딩 지원)
- 주파수 응답 그래프 PNG 스크린샷 저장
- 선호도 경계 오버레이
- 그래프 색상 커스터마이즈
- 초심자를 위한 주파수 영역 도움말
- 상세 측정값 확인을 위한 인스펙션 모드

### 사용자 인터페이스 & 커스터마이즈 기능
- 다국어 지원 & 다크 모드 지원
- 사용자 설정 가능한 요소 (링크, 워터마크, 설명)
- `theme.css`를 통한 그래프 테마 커스터마이즈 (+ [테마 생성기](/theme-generator))

### squig.link 연동
- squig.link 네트워크 전체에서 디바이스 교차 검색
- 해당 디바이스의 구매 링크
- 분석 연동
- 스폰서 배너 지원

## 내장 기능

각 기능에 대한 자세한 문서는 [기능](./category/features) 섹션을 참조하세요.

| 이름                   | 설명         |
|-----------------------|-------------|
| 이퀄라이저              | AutoEQ 및 실시간 오디오 재생이 포함된 파라메트릭 EQ 패널 |
| 디바이스 PEQ            | USB HID, Serial, Network 디바이스를 지원하는 하드웨어 EQ 브리지 |
| 타겟 커스터마이저         | 틸트, 베이스, 트레블 프리셋을 활용한 타겟 커브 커스터마이즈 |
| 선호도 경계              | 그래프에 표시되는 커스터마이즈 가능한 선호도 경계 오버레이 |
| 주파수 도움말            | 초심자를 위한 인터랙티브 주파수 영역 가이드 |
| 그래프 색상 휠           | 그래프 커브의 색상 및 외관 커스터마이즈 |
| squig.link 연동         | squig.link 네트워크를 위한 교차 검색, 구매 링크, 분석 기능 |
